import { RecommendationForm } from "@/components/RecommendationForm";

export const metadata = { title: "New recommendation — IndiaCharts Admin" };

export default function NewRecommendationPage() {
  return <RecommendationForm />;
}
