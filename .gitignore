node_modules
.next
out
build
*.log
.DS_Store

# env
.env
.env.local

# live MCP session token -- credential, never commit
.kite-mcp-session.json

# database
prisma/*.db
prisma/*.db-journal

# uploads
public/uploads/*
!public/uploads/.gitkeep

# saved wave analyses -- working files, not source
data/wave-analyses/*
!data/wave-analyses/.gitkeep

# standalone build output -- rebuild with scripts/build-artifact.mjs
artifact/wave-lab.html
