/**
 * Stub for Next.js's `server-only` marker.
 *
 * The real module is supplied by Next and exists to make a build fail when a
 * server module is imported into a client bundle. Vitest runs in Node with no
 * such boundary, so the import just needs to resolve to something.
 */
export {};
